# Проект: Backend + Frontend

Этот репозиторий содержит два приложения:

- **backend** — серверная часть (Docker)
- **frontend** — клиентское приложение (Angular)

Ниже приведены инструкции по запуску каждого из них.

---

## 📦 Запуск Backend

Backend работает через Docker Compose и после запуска будет доступен по адресу:

👉 **http://localhost:8080**

Также доступна документация Swagger:

👉 **http://localhost:8080/swagger-ui.html**

Перед запуском перейдите в папку:

```bash
cd backend
```

### ▶ Запуск

```bash
docker compose up -d
```

## 🖥 Запуск Frontend

Frontend — это Angular приложение и после запуска будет доступен по адресу:

👉 **http://localhost:4200**

Перед запуском перейдите в папку:

```bash
cd frontend
```

### 📥 Установка зависимостей

```bash
npm install
```

### ▶ Запуск в режиме разработки

```bash
npm run start
```
